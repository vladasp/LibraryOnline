@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
    @Styles.Render("~/Content/text.css")
}
<head>
    <title> </title>
</head>

<div class="Tables">
    <h3>Books data table</h3>
    @{
        var grid = new WebGrid(Model, canPage: true, rowsPerPage: 5, selectionFieldName: "selectedRow", ajaxUpdateContainerId: "gridContent");
        grid.Pager(WebGridPagerModes.NextPrevious);
    }

    <div id="gridContent">
        @grid.GetHtml(
                 tableStyle: "webGrid",
                 headerStyle: "header",
                 alternatingRowStyle: "alt",
                 selectedRowStyle: "select",
                 columns: grid.Columns(
                 grid.Column("ID", "Id"),
                 grid.Column("Author", "Author"),
                 grid.Column("Title", "Title")
        //grid.Column("Info", format: @<text>@Html.ActionLink(
        // (string)item.Story, "Info", "Home",
        // new { id = item.ID }, null)</text>))
        )
      )
        @*@if (grid.HasSelection)
            {
                using (MyModelDB context = new MyModelDB())
                {
                    client = context.Clients.Find(grid.SelectedRow[1]);
                }
                <b>Id</b> @client.ID<br />
                <b>Name</b>  @client.Name<br />
                <b>Aderess</b> @client.Adress<br />
            }*@


    </div>
</div>